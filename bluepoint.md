# Chezzle 项目设计文档

说明：以下文档中，“药品”即为“化学物质”或“化学药品”。

## 一、概述

**Chezzle**(Chemistry puzzle)是一款纯**H5**的2D小游戏，描述了用户在化学世界中通过进行各种化学反应制取“钥匙“并最终用钥匙打开出口。其界面风格和部分玩法设计类似于”冰火人“。

## 二、玩法

### 1.玩家

在每一关卡中，玩家都对应了一种化学物质（如$NaOH$,$Ca(OH)_2$)等（可能是固体，也可能是液体，取决于关卡开发者设定）。玩家的血量在本游戏中就是玩家所对应的物块的**质量**（一般的，玩家的初始质量是100g，但也可以取决于关卡设定）。

玩家可以通过`space`进行跳跃，通过`a`和`d`进行左右移动。当玩家当前所处的下端高度和前方地板高度相差较小时，无需按下跳跃键也可以顺利移动，距离较大时则会被挡住无法移动。（为了简化，一般将玩家的碰撞箱形状看作一个长方形。）此外，玩家可以通过`Shift`放置当前在物品栏中选中的沉淀（每次0.5g）。**放置的详细逻辑：一般来讲沉淀放置在脚下，但是如果附近有酒精灯（其实就是关卡内将沉淀吸附属性设置为真的物体），则将沉淀放置在酒精灯上。特别注意，当玩家脚下为容器（包括开关）时，沉淀会被放置在容器中。**放置的沉淀会生成在玩家脚下，具有碰撞箱，因此可以提升玩家高度。如果当前选中的格子没有沉淀，则不放置。玩家按下`q`可以捡起附近的沉淀。如果所有格子都满了，则不捡起沉淀。物品栏将在下文仔细讲述。

根据关卡设定，玩家可以推动物块，但是不可以推动物块跳动。玩家推动的物块重力不受限制，但是也要确保碰撞。

玩家属于特殊的物质，不会主动溶解在液体中（即使玩家自身是液体），除非玩家和所处环境可以发生反应。

玩家一般是一个椭圆形，但是也会随着反应而被侵蚀，因此玩家具体形状相当复杂。玩家的颜色也取决于物质的颜色，但是由于玩家（尤其作为固体时）常常不均匀，因此需要根据不同位置的物质颜色进行绘制。

当玩家是液体时，在地板上行走会少量损失自身重量。跳入烧杯中可以避免这一点，玩家在烧杯中可以带动烧杯进行移动，但是玩家无法携带烧杯跳跃。烧杯可以承载任何物质，无法被推翻。烧杯自身不参与任何化学反应，没有化学性质。

### 2.游戏场景

**Chezzle的游戏场景设计类似于“冰火人”。**

游戏场景主要包含以下内容：

a. 地板：一般来讲，一个关卡会有多层地板，地板不可移动也不具备化学性质，可以处于任何位置。根据关卡设计需要，距离上一层关卡距离较小的地板可以直接跳上，距离较大的需要通过其他特性向上移动（下文提及的沉淀搭高或通过气流等等）。

b. 玩家。

c. 药品池：药品池属于容器，这是游戏中化学药品出现最常见的形式，主要以液体药品形式存在。药品池在渲染上表现为比地面稍微低一些，其中存储着液体药品，玩家可以在走入时与其中物质反应（如果可以）。当然，反应之后，药品池中产生的沉淀（或一开始就有）也要在渲染中体现出来。在渲染时，药品池的颜色表现为其中溶液的颜色。

d. 物块：物块通常**具有化学性质**且可被推动，具体取决于关卡设定。物块具有碰撞箱（一般视作一个长方形），因此玩家可以站在上面。玩家不可以带动物块一起跳动。与玩家不同，物块会主动溶解（如果可溶），即使不会发生化学反应。**注意：物块一般具有化学性质，因此彼此之间可能相互发生反应（但是如果未溶解速度可能较慢），在药品池等液体环境中则更加易于反应。**

e. 开关，钥匙：钥匙本质上也是一类开关，且开关和钥匙本质上也都属于容器（仅限于需要用化学物质开启的）。开关通常需要某类化学物质才可以开启（但是根据关卡设定，有时候也可以设置成当玩家/物块在上时开启等等），这一物质被称作**开启物质**。当开关被开启之后，会触发其他物件的运动（比如，对于钥匙来说，就是通关口会打开），这不会在界面上体现，开关及其对应的事件需要玩家自己摸索。对于大多数开关，开启时会不断消耗其中存储的开启物质，消耗速度由关卡设定决定，当其中存储的开启物质消耗完之后会自动关闭。有些开关没有这一特性（如钥匙）。在渲染上，开关和钥匙不需要渲染其中的包含物，但是需要标注出开启物质剩余的量（为0g时无需标注）。

f. 通关口：这是玩家通关的位置。当钥匙处于开启状态且玩家位置在通关口附近时，判定玩家通关。

g. 玩家放置的沉淀：玩家放置的沉淀按照下文药品渲染方式进行渲染。玩家放置的沉淀具有重力，但是除了被重新收集之外不能被移动。

h. 其他物件：如烧杯等等。

关于容器的说明：容器的主要存在形式包括：药品池，烧杯，开关，酒精灯等等。容器中存放的物品通常会被渲染出来（会有一个渲染位置），但是对于一些特殊的容器（比如开关），存放的物品不渲染。通常来说，容器中存放物品的化学式会显示在一边。

### 3.游戏界面

游戏界面为2D画面，分为主游戏区和物品栏。

#### 所有药品（包括混合物和纯净物等等）的渲染

任何药品（包括混合物和纯净物）都要求依照其对应的物质的颜色进行渲染，无色物质则按照 **淡灰色（#AAA)** 的透明色进行渲染，浓度也会影响液体颜色和透明度。 **游戏中，很多固体并不均匀，因此同一个容器中的颜色也不只有一种。** 液体颜色直接取平均值，固体使用下文的“网格计算法”进行计算。一般的，沉淀以小圆的形式进行渲染（每0.1g一个小球，小球直径5px），并且颜色对应物质的颜色（见附录）。液体的透明度和颜色随着其中有色离子的浓度的变化而变化。

在容器中（包括药品池）中的液体渲染方式：主要部分由溶液所对应的颜色填充，中间参杂小的随机小球（每个2-10px)的缓慢浮动的小球，颜色比原本背景颜色深或是浅一些（随机）。

此外，药物所包含的内容需要以化学式形式显示在药品附近（关卡可以设置药品化学式是否显示）。

#### 主游戏区内容

主游戏区大小通常为w1000px，h800px，在无法完全显示的较小屏幕的时候进行等比例缩放。

地板：一般来讲，地板在被渲染时不会有遮挡物。

玩家：玩家一般渲染在最上层。玩家同样依照上文药物渲染方式渲染。

药品池：药品池所在位置的地板会有小幅度的凹陷，其中装载着药品。依照上文药物渲染方式进行渲染。

物块：物块通常是一中药品块，此时依照上文药物渲染方式进行渲染。当其不是药品块时，依照关卡设定进行渲染。

开关，钥匙：开关和钥匙都表现为一个**侧面的立体的**按钮，开关颜色根据关卡设计，钥匙的颜色总是橙红色。当开关/钥匙需要用化学物质解锁时，这一物质的化学式需要被显示在附近。开关还需要以数字形式显示出开启物质剩余的量。

通关口：通关口是一扇门的形状，表现为橙红色。

玩家信息HUD：在游戏区左上角，显示玩家当前质量。

提示：一个按钮，在游戏区右上角，点击显示当前提示。

控制：只在移动端出现，用于控制玩家动作：一个半透明灰色半圆的外侧被均匀分为5个部分，从左到右依次是向左，向左同时跳跃，向上跳跃，向右同时跳跃，向右移动。右方还有一个颜色透明度相同的按钮，上面画有向下箭头，意为向下放置沉淀。半圆部分逻辑：当玩家按住半圆部分时，视作开始控制，之后玩家可以将触控位置离开半圆部分，但是不能松开手，否则视作停止。将玩家触控位置和半圆中心连线，根据其与水平线的夹角进行判定。当玩家触控位置在半圆下方且处于触控状态时，将夹角视作0度或180度（根据玩家触控位置决定）。这个规则其实和大多数触控圈逻辑相同。

#### 物品栏内容

物品栏显示在主游戏区下方（能显示下）或右侧，不于主游戏区重叠。

物品栏由5个方框组成，展示了玩家收集的5种沉淀。每种沉淀占据一个格子（即按照物质种类区分），其中显示沉淀的物质类别和质量。每个格子最多可以承载100g沉淀物质。当前被玩家选中的格子边缘会有高亮。玩家通过鼠标点击来选中格子。

### 较为少见物品

常见物品在上文已经讲述。

#### 绳子

绳子在游戏中展现为一条细线。绳子有三个可以定义的量：悬挂位置（顶端），长度，悬挂物体。绳子每一帧会自动检查自己上方悬挂位置是否真的有物体。如果没有，那么绳子会断裂。绳子方向是始终向下的，不会弯曲。绳子自身没有碰撞箱。绳子下方悬挂的物体的位置完全由绳子决定。当绳子下方悬挂物体由于碰撞无法移动，绳子会断裂。

绳子断裂时，绳子会消失，下方物体重新受到重力作用。

#### 酒精灯

酒精灯通常使用一个开关控制。开关开启之后，酒精灯会燃烧，开关关闭时，酒精灯不会燃烧。注意：放置沉淀时，如果附近有酒精灯，沉淀会被放置在酒精灯上而非脚下。酒精灯也属于一种容器，其物品会在上方被渲染。注意：酒精灯提供”加热“的条件，并且可以点燃物体。

#### 酒精喷灯

酒精喷灯的其他特性与酒精灯相同，但是其提供”高温“的条件，也可以点燃物体。

#### 烧杯

烧杯属于容器。液体和固体都可以进入其中（包括玩家）。玩家在烧杯内外都可以推动烧杯左右移动，但是不能带动烧杯向上移动，向上跳跃时会跳出烧杯。

## 具体实现

### 游戏整体架构

为了确保游戏关卡设计的自由性，**chezzle的主体部分只是一个无法运行的库**，一个关卡是一个**完整的html文件，其中的js部分通过调用chezzle主体来实现关卡设计**。

chezzle主体部分分为：

a.`Scene`类：这是最主要的类，存储了整个游戏场景的所有内容。

b.`obj`类：游戏中每一个物体都是一个obj类。

b1.`player`类：玩家类，继承于obj

b2.`floor`类：地板类

b3.`pool`类：药品池类

b4.`blook`类：物块类（有化学性质的物块）

b5.`ublook`类：无化学性质的物块类

b6.`switch`类：开关类

b7.`key`类：钥匙类（继承于开关类）

b8.`door`类：通关口类

b9.`beaker`类：烧杯类

b10.`rope`类：绳子类

b11.`lamp`类：酒精灯类

b12.`gas`类：气泡柱类

b13.`container`类：容器类（`switch`、`key`、`beaker`、`pool`等属于容器的都继承于这个类）

注意：关卡在设计的时候还可以通过写别的类继承obj类来创建新种类的物品。

c.`line`类：底层类，用于定义一条线（通常平行或垂直地面）

d.`box`类：底层类，用于定义一个矩形

### 主体部分函数功能

注意：可以根据实现需要在此基础上自主添加。

#### Scene

函数：

a.`start()`：这是最重要的功能，放在一个关卡js代码的末尾，会阻塞主线程开始游戏循环。无返回值。

b.`add_solidbox(box)`：用于添加**实碰撞箱**。这类碰撞线通常用于描述地板。box是一个box类。无返回值。

c.`add_solidobj(obj)`：用于添加**实碰撞箱对象**。它的用法和`add_solidbox(box)`类似，区别和上面的**特别注意**部分相同。注意`add_solidbox(box)`很少用。无返回值。

d.`add_rpt(name,func)`：用于添加**刻运行函数**。**刻**是游戏运行的最小单位，通常是1/30s。rpt函数就是每一刻都会自动被执行的函数。name是这个刻运行函数的**唯一**标识符，func是一个函数，没有参数，是刻运行函数本体。返回值：`0`：添加成功；`1`：添加失败，原因是name重复。

e.`remove_rpt(name)`：用于删除刻运行函数。返回值：`0`：删除成功；`1`：删除失败，原因是name不存在。

f.`restart()`：重新开始游戏。这通常使用一个重定向来实现。无返回值。

g.`render()`：渲染整个游戏画面。无返回值。

h.`bind_line(line,action)`：用于绑定线碰撞事件。line代表被碰撞的线（line对象），action是一个函数，指示被碰撞之后执行的函数，其包含一个参数，代表碰撞line的对象。无返回值。

i.`bind_box(box,action)`：用于绑定矩形碰撞事件。box代表被碰撞的矩形（box对象），action是一个函数，指示被碰撞之后执行的函数，其包含一个参数，代表碰撞box的对象。无返回值。

j.`bind_obj_box(obj,action)`：用于绑定对象碰撞事件。obj代表被碰撞的对象（obj对象），action是一个函数，指示被碰撞之后执行的函数，其包含一个参数，代表碰撞obj的对象。无返回值。

**特别注意：不要弄混`bind_box(box,action)`与`bind_obj_box(obj,action)`。当描述一个固定不动的碰撞箱且没有对应obj对象的时候，才会使用`bind_box(box,action)`。通常，我们都使用`bind_obj_box(obj,action)`，因为它更加灵活，当obj移动的时候，用于判定的碰撞箱也会随之移动。因此，`bind_line(line,action)`和`bind_box(box,action)`其实很少用到。此外，这几个函数都是全局的，被任意物品碰撞都会触发。**

属性：

a.`tip`：一个字符串，代表当前游戏的提示（就是点击游戏界面右上角按钮显示的）

b.`ctx`：一个canvas对象。

c.`solid_boxes`：存储所有`add_solidbox(box)`添加的碰撞箱。

d.`solid_objs`：存储所有`add_solidobj(obj)`添加的碰撞对象。

e.`status`：一个字符串，指示游戏当前状态。"init"是Scene对象刚刚创建时的默认值。"running"是Scene.start()之后的值。"win"是玩家成功之后的值,"died"是玩家死亡之后的值。

f.`run_per_tick`：存储rpt函数的一个**字典**，键是rpt的唯一标识符，值是对应的函数。

g.`control_status`：存储玩家当前刻的控制信息，列表。比如，当玩家按下`a`时，列表中应当包含`"left"`项。注意：触控和键盘的`control_status`内容相同。

向左运动：`"left"` ；向右运动：`"right"` ；向上跳动：`"jump"` ；拾起沉淀：`"collect"`。 左上，右上移动时，将多个项目一并包含。

h.`line_binded`：一个列表

#### Obj

这个类逻辑相当复杂，可以自行添加其他有必要的内容。






## 附录

### 药品标准颜色

对于溶液而言，表中的浓度代表的是离子在水中达到饱和的浓度时的颜色。浓度降低时要注意调整颜色。


| 有颜色的药品名称 | 颜色    |
| ---------------- | ------- |
| $Cu^{2+}$        | #00e7ff |
| $Fe^{3+}$        | #ffbb00 |
| $Fe^{2+}$        | #11ff24 |
| $Cu(OH_2)$       | #00afff |
| $Fe(OH_3)$       | #002929 |
| $Fe（块状）$     | #fdfdfd |
| $Fe（粉末状）$   | #222222 |
| $CuO$            | #222222 |
| $Cu$             | #ff8f46 |
| $Fe_2O_3$        | #ff5f00 |
| $Fe_3O_4$        | #111111 |

### 需要支持的反应类型

初中范围内的化学反应，包括：

1.酸：酸与碱/盐/金属氧化物/金属（置换反应）

2.碱：碱与酸/盐/酸性气体；不溶性碱受热分解 （不考虑初中范围外的内容，如$Al(OH)_3$的某系特殊性质）

3.盐：盐与盐/酸/碱/金属（置换）

4.置换：只考虑金属阳离子（包含$H^+$）的置换。

5.氧化还原：只考虑：还原剂剂为$C$,$CO$,$H_2$；氧化剂为$Fe_2O_3$,$Fe_3O_4$,$CuO$的反应（但是包含$C+CO_2 \xlongequal {高温} 2CO$)6.其他：

6.氧气相关：燃烧（非金属单质，金属单质），$2H_2O_2\xlongequal{MnO_2}2H_2O+O_2 ↑$,$2KMnO_4 \xlongequal{加热} K_2MnO_4+MnO_2+O_2 ↑$,$2KClO_3\xlongequal{加热，MnO_2}2KCl+3O_2$

注意：$H_2CO_3$一律写作$H_2O+CO_2$。
